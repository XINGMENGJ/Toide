# Toide 本地开发用：Windows 版 Redis（社区构建），仅绑定本机。
# 工作目录请设为 tools/redis-windows（见 scripts/start-redis-windows.cmd）。

bind 127.0.0.1
port 6379

dir ./data
logfile ./redis.log

save 900 1
save 300 10
save 60 10000

appendonly yes
appendfilename "appendonly.aof"
